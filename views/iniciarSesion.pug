<!DOCTYPE html>
html(lang="en")
    head
        include partials/header


    body.login
        .contenedor-formulario
            h1 Iniciar Sesion en Uptask
            if locals.mensajes
                .alertas
                    each categoria in Object.keys(locals.mensajes)
                        each error in mensajes[categoria]
                            .alerta(class=`${categoria}`)
                                p= error
                
            
          
            form#formulario.caja.caja-login(method="POST" action="/iniciar-sesion")
                .campo
                    label(for="email") Email :
                    input(type="email" name="email" placeholder="Escribe tu Email" value= email )
                .campo
                    label(for="password") Password:
                    input(type="password" name="password" placeholder="Escribe tu Password" value= password )
                .campo.enviar
                    input.boton(type="submit" value="Iniciar sesion")
                .campo.acciones
                    a(href="/crear-cuenta") ¿Aun no tienes cuenta ? Crea una gratis

                                        a(href="/reestablecer") restablecer contraseña